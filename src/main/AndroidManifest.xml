<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecocity">

    <!-- Permisos para cámara, almacenamiento y geolocalización -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Especificamos que la aplicación usa la cámara, pero no es estrictamente necesaria -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".EcoCityApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="EcoCity"
        android:theme="@style/Theme.Ecocity">

        <!-- Actividad principal (Login) -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de registro de usuario -->
        <activity android:name=".RegisterActivity" />

        <!-- Actividad de incidencias -->
        <activity android:name=".IncidenciaActivity" />

        <!-- Actividad para mostrar la lista de incidencias -->
        <activity android:name=".ListaIncidenciasActivity" />

        <!-- Actividad para tomar fotos -->
        <activity android:name=".GeolocalizacionActivity" />

        <!-- Chat de soporte TCP/IP -->
        <activity android:name=".SupportChatActivity" />

        <!-- Permiso para usar el servicio de Google Maps -->
        <meta-data
            android:name="com.google.android.gms.maps.v2.API_KEY"
            android:value="@string/google_maps_api_key" />

        <!-- FileProvider para compartir archivos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.ecocity.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
